%h1 Team View
%p
  = team_size_dialog
= team_capacity_check

- @style_team.each do |user|

  - if user.followed_by(current_user)
    = render partial: "shared/stylist_listing_tiles", locals: { user: user }
